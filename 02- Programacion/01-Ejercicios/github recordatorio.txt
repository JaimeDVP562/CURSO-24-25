

--------------------------------------------------------------------------------------
                 SUBIDA
--------------------------------------------------------------------------------------


git status

git commit -m "Descripción de los cambios"

git pull origin main

git push origin main


--------------------------------------------------------------------------------------
                 DESCARGA
--------------------------------------------------------------------------------------

git status

git pull origin main

--------------------------------------------------------------------------------------
                 CLONAR UN REPOSITORIO EN OTRO EQUIPO
--------------------------------------------------------------------------------------

	-> copia la url del repositorio que quieres clonar
	-> vas al sitio donde necesitas clonar el repositorio 
	-> acceder a la cmd desde dicha carpeta 
	-> git clone URL del repositorio 


--------------------------------------------------------------------------------------
                 HACER UNA CARPETA REPOSITORIO EN UN EQUIPO
--------------------------------------------------------------------------------------

	-> crea un repositorio en GitHub y copia la direccion del repositorio
	-> accede a la carpeta que quieres hacer repositorio 
	-> en la barra de direcciones de la misma carpeta escribe  cmd para acceder a 
	la ubicacion de la carpeta en la cmd
	-> ejecuta el comando:  git init --> esto inicializa git en la carpeta
	-> ejecuta el comando:  git remote add origin (la direccion que habrás copiado del repositorio remoto creado en GitHub)
	-> ejecuta el comando:  git add .--> esto añade los archivos que tienes en el repositorio local al repositorio remoto
	-> ejecuta el comando:  git commit -m "Primer commit" --> esto añade un comentario (con los cambios realizados en el repositorio) cada vez que subas un archivo modificado o actualices tu repositorio digamos 
	-> ejecuta el comando:  git push -u origin main --> esto sube los cambios a la rama principal 
 






